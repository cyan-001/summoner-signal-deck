# 「峡谷声纹」发布说明

## 任务目标

将当前项目完整发布到 GitHub。默认创建私有仓库，仓库名为 `summoner-signal-deck`。除非用户另有说明，不要改动页面、音效或现有 Sites 部署。

## 项目信息

- 本地路径：`/Users/cyan/AI/创意网页`
- 技术栈：Vinext + React 19 + TypeScript + Tailwind CSS
- Node.js 要求：`>=22.13.0`
- 当前线上站点：<https://summoner-signal-deck.screenbks.chatgpt.site>
- Sites 项目配置：`.openai/hosting.json`

## 音效对应关系

已发布的音效文件位于 `public/audio/`，必须随源码一起推送：

1. `1.Enemy_Missing_ping_SFX.ogg` —— 问号标记
2. `2.Announcer_Female1_114.ogg` —— 胜利
3. `3.Zhonya's_Hourglass_active_SFX.ogg` —— 时间沙漏
4. `4.Heartsteel_trigger_SFX.ogg` —— 心之钢

根目录的 `音效/` 是用户本地投放目录，已被 `.gitignore` 忽略；不要再把它重复上传。

## 发布前检查

在项目根目录执行：

```bash
npm ci
npm run build
git status --short
```

构建必须成功。不要提交 `node_modules/`、`dist/`、`.vinext/` 或本地环境文件。

## GitHub 发布流程

1. 确认用户的 GitHub 账号已授权。
2. 确认项目当前变更已提交；若这份说明文件尚未提交，先提交它。
3. 在 GitHub 创建私有仓库 `summoner-signal-deck`，不要在远程预生成 README、`.gitignore` 或 License。
4. 将当前本地 `main` 分支推送到新仓库。
5. 如果需要添加 Git remote，优先命名为 `github`，避免与 Sites 的源仓库流程混淆。
6. 推送后检查 GitHub 仓库中至少存在：`app/`、`public/audio/`、`package.json`、`package-lock.json` 和本文件。
7. 返回 GitHub 仓库链接，并明确说明仓库为私有。

若使用 GitHub CLI，在已登录的前提下可使用：

```bash
gh repo create summoner-signal-deck --private --source=. --remote=github --push
```

## 重要注意事项

- 不要将 GitHub token、Sites 凭据或其他密钥写入文件、Git remote URL 或提交记录。
- 音效来自用户提供的游戏素材。若要将仓库转为公开，应先由用户确认相关授权与分发条件。
- 本次任务只是上传 GitHub，不需要重新创建 Sites 项目，也不要更改 `.openai/hosting.json` 中的 `project_id`。
